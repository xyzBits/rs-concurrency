use anyhow::{anyhow, Result};
use std::fmt::Debug;
use std::ops::Add;
use std::ops::{AddAssign, Mul};

fn main() -> Result<()> {
    println!("{}", f64::default());

    let res: f32 = Default::default();

    Ok(())
}
